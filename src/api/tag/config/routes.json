{
  "routes": [
    {
      "method": "GET",
      "path": "/tags",
      "handler": "tag.find",
      "config": {
        "policies": ["global::disable"]
      }
    },
    {
      "method": "GET",
      "path": "/tags/count",
      "handler": "tag.count",
      "config": {
        "policies": ["global::disable"]
      }
    },
    {
      "method": "GET",
      "path": "/tags/:id",
      "handler": "tag.findOne",
      "config": {
        "policies": ["global::disable"]
      }
    },
    {
      "method": "POST",
      "path": "/tags",
      "handler": "tag.create",
      "config": {
        "policies": ["global::disable"]
      }
    },
    {
      "method": "PUT",
      "path": "/tags/:id",
      "handler": "tag.update",
      "config": {
        "policies": ["global::disable"]
      }
    },
    {
      "method": "DELETE",
      "path": "/tags/:id",
      "handler": "tag.delete",
      "config": {
        "policies": ["global::disable"]
      }
    }
  ]
}
